spring:
  banner:
    location: 'classpath:DirectProject.txt'
  main:
    allow-bean-definition-overriding: true
    log-startup-info: false

#Default the security credentials to call the config service. Override these
#for your deployment.
direct:
  webservices:
    security:
      basic:
        user:
          name: admin
          password: d1r3ct;
          
  config:
    service:
      url: http://localhost:8082/       